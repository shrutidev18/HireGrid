import PhasePlaceholder from '../components/PhasePlaceholder';

export default function Applications() {
  return (
    <PhasePlaceholder
      title="Applications"
      description="Every application you are tracking, searchable and filterable."
      phase="a later phase"
    />
  );
}
